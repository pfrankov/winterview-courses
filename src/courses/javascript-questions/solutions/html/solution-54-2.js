module.exports = () => `Когда мы устанавливаем <code>y</code> равным<code> 10</code>, мы фактически добавляем свойство <code>y</code> к глобальному объекту (<code>window</code> в браузере, <code>global</code> в Node). В браузере <code>window.y</code> теперь равен<code> 10</code>.

Затем мы объявляем переменную <code>x</code> со значением <code>y</code>, которое равно <code>10</code>. Переменные, объявленные с ключевым словом <code>let</code>, имеют <i>блочную видимость</i>, они определены только в блоке, в котором они объявлены; немедленно вызванная функция (IIFE) в этом случае. Когда мы используем оператор <code>typeof</code>, операнд <code>x</code> не определен: мы пытаемся получить доступ к <code>x</code> вне блока, в котором он объявлен. Это означает, что <code>x</code> не определен. Значения, которым не присвоено или не объявлено значение, имеют тип <code>"undefined"</code>. <code>console.log(typeof x)</code> возвращает <code>"undefined"</code>.

Однако мы создали глобальную переменную <code>y</code>, установив <code>y</code> равным <code>10</code>. Это значение доступно в любом месте нашего кода. <code>y</code> определен и содержит значение типа <code>"number"</code>. <code>console.log(typeof y)</code> возвращает <code>"number"</code>.
`;