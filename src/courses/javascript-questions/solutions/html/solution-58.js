module.exports = () => `Оператор <code>delete</code> возвращает логическое значение: <code>true</code> при успешном удалении, иначе он вернет <code>false</code>. Однако переменные, объявленные с ключевым словом <code>var</code>,<code> const</code> или <code>let</code>, не могут быть удалены с помощью оператора<code> delete</code>.

Переменная <code>name</code> была объявлена ​​с ключевым словом <code>const</code>, поэтому ее удаление не было успешным: возвращается <code>false</code>. Когда мы устанавливаем <code>age</code> равным <code>21</code>, мы фактически добавляем свойство с именем <code>age</code> к глобальному объекту. Вы можете успешно удалить свойства из объектов, в том числе из глобального объекта, поэтому <code>delete age</code> возвращает <code>true</code>.
`;