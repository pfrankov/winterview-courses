module.exports = () => `Обычно, когда мы устанавливаем объекты равными <code>null</code>, эти объекты получают метку <i>собрано в мусор</i>, так как больше нет ссылок на этот объект. Однако, поскольку функция обратного вызова в <code>setInterval</code> является стрелочной функцией стрелки (таким образом, привязанной к объекту <code>config</code>), функция обратного вызова все еще содержит ссылку на объект <code>config</code>. Пока есть ссылка, объект не будет собран в мусор. Поскольку сборщик мусора не отрабатывает, функция обратного вызова <code>setInterval</code> будет по-прежнему вызываться каждые 1000 мс (1с).
`;