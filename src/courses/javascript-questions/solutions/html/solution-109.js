module.exports = () => `Мы устанавливаем значение свойства <code>favourFood</code> для объекта <code>info</code> равным строке со смайликами для пиццы, <code>'🍕'</code>. Строка является примитивным типом данных. В JavaScript примитивные типы данных передаются по ссылке ...

В JavaScript примитивные типы данных (все, что не является объектом) передаются как <i>значение</i>. В этом случае мы устанавливаем значение свойства <code>favourFood</code> объекта <code>info</code> равным значению первого элемента в массиве <code>food</code>, в данном случае это строка с emoji пиццы (<code>'🍕'</code>). Строка является примитивным типом данных и взаимодействует по значению (см. мой <a href="https://www.theavocoder.com/complete-javascript/2018/12/21/by-value-vs-by-reference">пост в блоге</a>, если вы заинтересованы в получении дополнительной информации).

Затем мы меняем значение свойства <code>favourFood</code> объекта <code>info</code>. Массив <code>food</code> не изменился, поскольку значение <code>favourFood</code> было просто <i>скопировано</i> из значения первого элемента в массиве и не имеет ссылки на то же место в памяти, что и элемент на <code>food[0]</code>. Когда мы выводим в лог <code>food</code>, это все равно исходный массив, <code>['🍕', '🍫', '🥑', '🍔']</code>.
`;