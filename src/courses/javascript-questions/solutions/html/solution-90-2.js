module.exports = () => `Вызов конструктора функции с <code>new</code> приводит к созданию экземпляра <code>Person</code>, ключевое слово <code>typeof</code> возвращает <code>"object"</code> для экземпляра. <code>typeof member</code> возвращает <code>"объект"</code>.
`;