module.exports = () => `Это означает, что <code>a + b</code> никогда не достигается, так как функция перестает выполняться после ключевого слова <code>return</code>. Если значение не возвращается, как здесь, функция возвращает значение <code>undefined</code>. Обратите внимание, что после операторов <code>if / else</code> автоматической вставки нет!
`;