module.exports = () => `С помощью метода <code>defineProperty</code> мы можем добавлять новые свойства к объекту или изменять существующие. Когда мы добавляем свойство к объекту с помощью метода <code>defineProperty</code>, они по умолчанию <i>не перечисляемые</i>. Метод <code>Object.keys</code> возвращает все имена <i>enumerable</i> свойств объекта, в данном случае только <code>"name"</code>.

Свойства, добавленные с помощью метода <code>defineProperty</code>, по умолчанию неизменны. Вы можете переопределить это поведение, используя свойства <code>writable</code>, <code>configurable</code> и <code>enumerable</code>. Таким образом, метод <code>defineProperty</code> дает вам гораздо больший контроль над свойствами, которые вы добавляете к объекту.
`;