module.exports = () => `Значение <code>res</code> во втором <code>.then</code> равно возвращенному значению предыдущего <code>.then</code>. Вы можете продолжать цепочку <code>.then</code> таким образом; значение передается следующему обработчику.
`;