module.exports = () => `Когда мы передаем несколько промисов методу <code>Promise.race</code>, он разрешает/отклоняет <i>первый</i> промис, который разрешается/отклоняется. В метод <code>setTimeout</code> мы передаем таймер: 500 мс для первого промиса (<code>firstPromise</code>) и 100 мс для второго промиса (<code>secondPromise</code>). Это означает, что <code>secondPromise</code> разрешается первым со значением <code>'два'</code>. <code>res</code> теперь содержит значение <code>'два'</code>, которое выводиться в консоль.
`;