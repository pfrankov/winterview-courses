module.exports = () => `Значение ключевого слова <code>this</code> зависит от того, где вы его используете. В <b>методе</b>, как и в методе <code>getStatus</code>, ключевое слово <code>this</code> ссылается на объект, которому принадлежит метод. Метод принадлежит объекту <code>data</code>, поэтому <code>this</code> относится к объекту <code>data</code>. Когда мы регистрируем <code>this.status</code>, регистрируется свойство <code>status</code> объекта <code>data</code>, которое является <code>"🥑"</code>.

С помощью метода <code>call</code> мы можем изменить объект, на который ссылается ключевое слово <code>this</code>. В <b>функциях</b> ключевое слово <code>this</code> относится к <i>объекту, которому принадлежит функция</i>. Мы объявили функцию <code>setTimeout</code> для объекта <i>global</i>, поэтому в функции <code>setTimeout</code> ключевое слово <code>this</code> ссылается на объект <i>global</i>. В глобальном объекте есть переменная с именем <i>status</i> со значением <code>"😎"</code>. При регистрации <code>this.status</code> выводится <code>"😎"</code>.
`;