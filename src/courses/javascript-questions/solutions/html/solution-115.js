module.exports = () => `При добавлении пары ключ/значение с использованием метода <code>set</code> имя ключа будет равно значению первого аргумента, переданного в функцию <code>set</code>, а значением будет второй аргумент, переданный в функцию <code>set</code>. В данном случае ключом является <i>функция</i> <code>() => 'greeting'</code> и значение <code>'Hello world'</code>. <code>myMap</code> теперь это <code>{ () => 'greeting' => 'Hello world!' }</code>.

1 неверно, поскольку ключ не <code>'greeting'</code>, а <code>() => 'greeting'</code>.
3 неверно, так как мы создаем новую функцию, передавая ее в качестве параметра методу <code>get</code>. Объект взаимодействует со <i>ссылкой</i>. Функции - это объекты, поэтому две функции никогда не бывают строго равными, даже если они идентичны: они имеют ссылки на разные места в памяти.
`;