module.exports = () => `С синтаксисом <code>import * as name</code> мы импортируем <i>все exports</i> из файла <code>module.js</code> в файл <code>index.js</code>, тогда и создается новый объект с именем <code>data</code>. В файле <code>module.js</code> есть два экспорта: экспорт по умолчанию и именованный экспорт. Экспорт по умолчанию - это функция, которая возвращает строку <code>"Hello World"</code>, а именованный экспорт - это переменная с именем <code>name</code>, которая имеет значение строки <code>"Lydia"</code>.

Объект <code>data</code> имеет свойство <code>default</code> для экспорта по умолчанию, другие свойства имеют имена именованных экспортов и их соответствующие значения.
`;