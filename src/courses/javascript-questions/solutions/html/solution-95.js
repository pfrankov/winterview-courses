module.exports = () => `В JavaScript мы <i>не должны</i> явно указывать точку с запятой (<code>;</code>), однако движок JavaScript все равно добавляет их после операторов. Это называется <b>автоматической вставкой точек с запятой</b>. Например, оператором могут быть переменные или ключевые слова, такие как <code>throw</code>, <code>return</code>, <code>break</code> и т.д.

Здесь мы написали инструкцию <code>return</code> и другое значение <code>a + b</code> в новой строке. Однако, поскольку это новая линия, движок не знает, что это на самом деле значение, которое мы хотели бы вернуть. Вместо этого он автоматически добавляет точку с запятой после <code>return</code>. Вы можете увидеть это как:
`;