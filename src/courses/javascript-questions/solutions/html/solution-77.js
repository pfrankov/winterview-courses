module.exports = () => `Чистая функция - это функция, которая всегда возвращает тот же результат, если переданы одинаковые аргументы.

Функция <code>sum</code> всегда возвращает один и тот же результат. Если мы передадим <code>1</code> и <code>2</code>, он всегда вернет <code>3</code> без побочных эффектов. Если мы передадим <code>5</code> и <code>10</code>, он всегда вернет <code>15</code> и так далее. Это определение чистой функции.
`;