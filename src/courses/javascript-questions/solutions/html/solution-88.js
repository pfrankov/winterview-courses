module.exports = () => `Вы можете установить значение параметра по умолчанию равным другому параметру функции, если они были определены <i>до</i> параметров по умолчанию. Мы передаем значение <code>10</code> в функцию <code>sum</code>. Если функция <code>sum</code> принимает только один аргумент, это означает, что значение для <code>num2</code> не передано, и в этом случае значение <code>num1</code> равно переданному значению <code>10</code>. Значением по умолчанию <code>num2</code> является значение <code>num1</code>, которое равно <code>10</code>. <code>num1 + num2</code> возвращает <code>20</code>.

Если вы пытаетесь установить значение параметра по умолчанию равным параметру, который определен <i>после</i> (справа), то значение параметра еще не было инициализировано; это приведет к ошибке.
`;