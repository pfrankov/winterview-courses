module.exports = () => `Используя метод <code>splice</code>, мы модифицируем исходный массив, удаляя, заменяя или добавляя элементы. В этом случае мы удалили 2 элемента из индекса 1 (мы удалили <code>'🥑'</code> и <code>'😍'</code>) и добавили <code>✨</code> emoji.

<code>map</code>, <code>filter</code> и <code>slice</code> возвращают новый массив, <code>find</code> возвращает элемент, а <code>reduce</code> возвращает аккумулированное значение.
`;