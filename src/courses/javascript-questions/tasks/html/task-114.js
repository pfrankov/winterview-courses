module.exports = () => `<b>Что произойдет?</b>

- A: обратный вызов <code>setInterval</code> не будет вызван
- B: обратный вызов <code>setInterval</code> будет вызван один раз
- C: обратный вызов <code>setInterval</code> будет вызываться каждую секунду
- D: мы никогда не вызовем <code>config.alert()</code>, т.к. <code>config</code> равно <code>null</code>
`;